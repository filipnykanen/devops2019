version: 0.2

phases:
  install:
    run-as: root
      runtime-versions:
        nodejs: 10
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - npm run package
artifacts:
  files:
    - "**/*"
  base-directory: dist
